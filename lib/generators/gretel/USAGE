Description:
  Generates a sample breadcrumbs configuration file for Gretel.

Example:
  rails generate gretel:install
  or
  rails generate gretel:install --from-routes --link-text-attr-name name

  This will create:
    config/breadcrumbs.rb
